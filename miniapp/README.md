# Prompix Miniapp Workspace

这个目录预留给微信小程序工程（长期上线目标）。

## 计划建议
- 技术路线：优先 Taro（React 语法迁移成本低）
- 目录职责：`miniapp/` 仅存放微信小程序代码与构建配置
- Web 与小程序共用逻辑：优先抽离到 `packages/core`（下一阶段再做）

## 当前状态
- 还未初始化小程序工程，因此 `npm run start:weapp` 会提示未初始化。

## 下一步（建议）
1. 在 `miniapp/` 初始化 Taro 项目。
2. 将图片上传、分析请求、设置存储这三条主链路先迁移到小程序。
3. 完成微信开发者工具调试与真机预览。
